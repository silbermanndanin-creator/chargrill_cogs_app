# Pinned to an exact, verified-compatible set (resolved together on Python 3.12).
# Streamlit Cloud hit pip ResolutionImpossible with open-ended `>=` pins when new
# releases shifted requirements — exact pins make every deploy reproducible.
# To upgrade: bump a pin, confirm `pip install -r requirements.txt` resolves, deploy.
streamlit==1.58.0
anthropic==0.109.1
pandas==3.0.3
openpyxl==3.1.5   # read Payroll Setup.xlsx + build payroll & reconciliation Excel
python-calamine==0.6.2   # robust reader for Tyro location reports (Reconciliation tab)
requests==2.34.2
pydantic==2.13.4
Pillow==12.2.0   # resize/compress invoice & POS photos before upload (speed)
plotly==6.8.0
python-docx==1.2.0   # generate part-time variation letters (.docx) from the firm template
supabase==2.31.0   # only used in cloud mode (SUPABASE_URL + SUPABASE_KEY set); local uses CSV
holidays==0.98   # public-holiday detection for the drinks order (state-aware)
google-api-python-client==2.197.0   # optional: save variation letters to Google Drive
google-auth==2.53.0                 # service-account auth for the Drive upload
